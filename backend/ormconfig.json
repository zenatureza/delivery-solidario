{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "delivery-solidario",
  "username": "postgres",
  "password": "docker",
  "logging": false,
  "entities": ["./src/models/**/*.ts"],
  "migrations": ["./src/database/migrations/**/*.ts"],
  "cli": {
    "entitiesDir": "./src/models",
    "migrationsDir": "./src/database/migrations"
  }
}
